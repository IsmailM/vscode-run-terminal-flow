# vscode-run-terminal-flow README

Simply run selection in the VSCODE terminal and move cursor to next line

```bash
npm install -g yo generator-code
npm install -g vsce

vsce package

code --install-extension ./vscode-run-terminal-flow-0.0.1.vsix
```
